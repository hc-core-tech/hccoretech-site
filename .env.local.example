# Resend API key for the quote form. Get one at https://resend.com/api-keys
# For local dev, add your key to .env.local (never commit that file).
# For production, add this variable in the Vercel dashboard under Project Settings > Environment Variables.
RESEND_API_KEY=re_your_api_key_here